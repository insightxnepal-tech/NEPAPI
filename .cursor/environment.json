{
  "name": "NEPSE API",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "rest-api",
      "command": ".venv/bin/python -m uvicorn server:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI REST server for NEPSE market data (http://localhost:8000, docs at /docs)."
    },
    {
      "name": "websocket",
      "command": ".venv/bin/python socketServer.py",
      "description": "WebSocket server streaming NEPSE data on ws://localhost:5555."
    }
  ],
  "ports": [
    { "name": "rest-api", "port": 8000 },
    { "name": "websocket", "port": 5555 }
  ]
}
